@{
    ViewBag.Title = "Home Page";
}

<div id="content">
</div>

<script type="text/template" id="event-template">
<div class="hero-unit">    
    <h1><%= event.get('Title') %></h1>
    
    <%= event.get('Description') %>

    <a class="btn btn-primary btn-large" href="<%= event.get('RegistrationLink') %>">Inscreva-se</a>

    <div class="social-btns">
        @Html.Partial("SocialButtons")
    </div>
</div>
</script>